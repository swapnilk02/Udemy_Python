# a python standard library has wide range of module that we can use .......some of this module we can use without
# explicitely importing them (they are imported automatically)...while some we need to import ....ex

# the module that are imported automatically are can be seen using the dir command like below

print(dir())    # this will give  the list of the module that are imported automatically


# as the result of the above line shows .....if the name of something starts with "__"...it  means that particular
#thing is not supposed to be changed....though we can change it if we want


# one of the module that is automatically imported is builtins
# this module contain the list of the object some built in python functions
